#include "main.h"

/**
 * main - check the code
 * _strcat - retrun char
 * @dest: parameter
 * @src: parameter2
 * Return: Always 0.
 */
char *_strcat(char *dest, char *src)
{
    char * str = *dest + *src;
    return str;
}
